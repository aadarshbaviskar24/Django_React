version: 1.0.0  # ✅ Required field (was missing)
schemaVersion: "1.2"

endpoints:
  - name: rest-api-be2
    displayName: REST API
    type: REST  # ✅ Required field
    context: /  # ✅ Required for REST-type endpoints
    port: 8000  # ✅ Required field
    service:
      basePath: /
    networkVisibilities:
      - Project
      - Public
